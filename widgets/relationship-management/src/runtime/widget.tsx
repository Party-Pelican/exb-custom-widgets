import { React, type AllWidgetProps } from "jimu-core";
import type { IMConfig } from "../config";

const Widget = (props: AllWidgetProps<IMConfig>) => {
  return (
    <div>
      <p>Relationship Management Widget</p>
    </div>
  );
};

export default Widget;
